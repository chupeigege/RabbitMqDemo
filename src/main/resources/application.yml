spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        retry:
          enabled: false
        acknowledge-mode: manual
    publisher-confirms: true
    publisher-returns: true